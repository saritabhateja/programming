# Using case
UPDATE salary 
    SET sex = (CASE WHEN sex='m'
                        then 'f'
                        else 'm'
                        END)

# Using if
UPDATE salary 
    SET sex = IF(sex='m','f','m')
